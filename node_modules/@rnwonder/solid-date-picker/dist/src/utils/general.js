import { p as c } from "../../node_modules/.pnpm/@rnwonder_simple-datejs@1.0.8/node_modules/@rnwonder/simple-datejs/dist/format-CMbjATj9.js";
const i = (t, n) => {
  const u = (e) => {
    if (!isNaN(Number(e)))
      return Number(e);
    for (let r = 0; r <= 9; r++)
      if (c(r, t) === e)
        return r;
    throw new Error(`Unrecognized digit '${e}' for locale '${t}'`);
  };
  let o = 0, l = 1;
  const a = n.value.replace(/[,٬.’  '\s]/g, "");
  for (let e = a.length - 1; e >= 0; e--)
    o += u(a[e]) * l, l *= 10;
  return { ...n, value: o };
};
export {
  i as convertFormattedNumberBackToNumber
};
