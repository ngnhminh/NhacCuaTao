import { Locale } from './types';
import { CustomAccessor } from './generate';

export declare const getAccessorValue: <T>(accessor: CustomAccessor<T>) => T;
export declare const numberFormatter: (num: number, locale?: Locale) => string;
export declare const modifiedDate: (year: number, month: number, day: number | undefined) => Date;
